#include "util/window.h"

#include <stdio.h>

#define WINDOW_WIDTH 600
#define WINDOW_HEIGHT 600


int main() {
    printf("Successfully loaded main.\n");
    runWindow(WINDOW_WIDTH, WINDOW_HEIGHT);
    return 0;
}
